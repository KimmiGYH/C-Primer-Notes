练习6.52：已知有如下声明：

```cpp
void manip(int, int);
double dobj;

// 请指出下列调用中每个类型转换的等级

(a) manip('a', 'z');
// (a)发生的参数类型转换是类型提升，字符型实参自动提升成整型。

(b) manip(55.4, dobj);
// (b)发生的参数类型转换是算术类型转换，双精度浮点型自动转换成整型
```
